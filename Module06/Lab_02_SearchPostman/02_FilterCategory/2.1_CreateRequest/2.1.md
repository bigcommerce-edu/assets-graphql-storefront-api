Request name:
GQL Product Search - Cat 

(Duplicate GQL Product Search)

Body:
```
query Search(
    $searchTerm: String,
    $categoryId: Int!
) {
    site {
        search {
            searchProducts(
                filters: {
                    searchTerm: $searchTerm,
                    categoryEntityId: $categoryId
                }
            ) {
              ... // LEAVE QUERIED FIELDS UNCHANGED //
            }
        }
    }
}
```

Variables:
```
{
    "searchTerm": "{{filter_search_term}}",
    "categoryId": {{filter_category}}
}
```