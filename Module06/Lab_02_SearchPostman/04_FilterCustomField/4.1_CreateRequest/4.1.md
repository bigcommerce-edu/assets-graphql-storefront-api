Request name:
GQL Product Search - Cat, Variant, Field 

(Duplicate GQL Product Search - Cat, Variant)

Body:
```
query Search(
    $searchTerm: String,
    $categoryId: Int!,
    $variantAttrName: String!,
    $variantAttrVal: String!,
    $customFieldName: String!,
    $customFieldVal: String!
) {
    site {
        search {
            searchProducts(
                filters: {
                    searchTerm: $searchTerm,
                    categoryEntityId: $categoryId,
                    productAttributes: [
                        {
                            attribute: $variantAttrName,
                            values: [$variantAttrVal]
                        },
                        {
                            attribute: $customFieldName,
                            values: [$customFieldVal]
                        }
                    ]
                }
            ) {
              ... // LEAVE QUERIED FIELDS UNCHANGED //
            }
        }
    }
}
```

Variables:
```
{
    "searchTerm": "{{filter_search_term}}",
    "categoryId": {{filter_category}},
    "variantAttrName": "{{filter_variant_attr_name}}",
    "variantAttrVal": "{{filter_variant_attr_val}}",
    "customFieldName": "{{filter_custom_field_name}}",
    "customFieldVal": "{{filter_custom_field_val}}"
}
```